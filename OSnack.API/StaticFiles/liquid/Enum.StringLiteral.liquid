{% if HasDescription -%}
/** {{ Description }} */
{% endif -%}
export type {{ Name }} = {% for enumeration in Enums -%}{% if Enums.first.Value != enumeration.Value %} | {% endif %}{{ enumeration.Value }}{% endfor -%};